*{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
a{color:inherit;text-decoration:none}
img{max-width:100%;display:block}
:focus-visible{outline:3px solid var(--flame);outline-offset:3px}

:root{
  --flame:#F1620D; --flameL:#F79512; --ember:#DF2E1B;
  --blue:#0A6FCC; --blueL:#12A6F0; --blueD:#0546B0;
  --black:#000; --graphite:#14181C; --panel:#191F25;
  --ash:#8A97A4; --cloud:#F2F4F6; --line:rgba(255,255,255,.10);
}
body{font-family:'Archivo',sans-serif;background:var(--black);color:var(--cloud);line-height:1.65;-webkit-font-smoothing:antialiased}
.wrap{width:100%;max-width:1180px;margin:0 auto;padding:0 28px}
h1,h2,h3{font-weight:800;letter-spacing:-.02em;line-height:1.08}
.eyebrow{display:inline-block;font-size:11.5px;font-weight:700;letter-spacing:.24em;text-transform:uppercase;
  background:linear-gradient(90deg,var(--flameL),var(--ember));-webkit-background-clip:text;background-clip:text;color:transparent}
.sec{padding:96px 0}
.sec-head{max-width:660px;margin-bottom:52px}
.sec-head h2{font-size:clamp(30px,4vw,46px);margin-top:14px}
.sec-head p{color:var(--ash);font-size:17px;margin-top:16px}
.skip{position:absolute;left:-9999px}
.skip:focus{left:12px;top:12px;z-index:100;background:#fff;color:#000;padding:10px 16px;border-radius:6px}

.btn{display:inline-flex;align-items:center;justify-content:center;gap:9px;font-weight:700;font-size:15px;padding:16px 30px;border-radius:8px;border:2px solid transparent;cursor:pointer;transition:transform .15s,box-shadow .15s,border-color .15s,color .15s}
.btn-flame{background:linear-gradient(180deg,var(--flameL),var(--ember));color:#fff;box-shadow:0 10px 30px -12px rgba(241,98,13,.8)}
.btn-flame:hover{transform:translateY(-2px);box-shadow:0 16px 40px -12px rgba(241,98,13,.95)}
.btn-ghost{border-color:rgba(255,255,255,.25);color:#fff}
.btn-ghost:hover{border-color:var(--blueL);color:var(--blueL)}

/* Header */
header{position:sticky;top:0;z-index:60;background:rgba(0,0,0,.86);backdrop-filter:blur(12px);border-bottom:1px solid var(--line)}
.nav{display:flex;align-items:center;justify-content:space-between;height:82px;gap:20px}
.nav .logo{height:46px;width:auto}
nav ul{display:flex;gap:26px;list-style:none;align-items:center}
nav a{font-size:14.5px;font-weight:600;color:#D5DBE1;transition:color .15s}
nav a:hover,nav a[aria-current=page]{color:var(--blueL)}
.nav-cta{display:flex;align-items:center;gap:16px}
.phone-link{font-weight:800;font-size:16px;white-space:nowrap;color:#fff}
.phone-link span{background:linear-gradient(90deg,var(--flameL),var(--ember));-webkit-background-clip:text;background-clip:text;color:transparent}
.burger{display:none;flex-direction:column;gap:5px;background:none;border:none;cursor:pointer;padding:8px;color:#fff}
.burger span{width:26px;height:2.5px;border-radius:2px;background:currentColor}
.mobile-menu{display:none;background:var(--graphite);border-bottom:1px solid var(--line)}
.mobile-menu.open{display:flex;flex-direction:column;padding:16px 28px 24px}
.mobile-menu a{padding:12px 0;font-weight:600;border-bottom:1px solid var(--line)}
.mobile-menu .btn{margin-top:14px}

/* Hero (home) */
.hero{position:relative;overflow:hidden;text-align:center;padding:92px 0 84px}
.hero::before{content:"";position:absolute;left:50%;top:-10%;transform:translateX(-50%);width:900px;height:640px;
  background:radial-gradient(ellipse at center,rgba(10,111,204,.28),transparent 62%);pointer-events:none}
.hero::after{content:"";position:absolute;left:50%;top:26%;transform:translateX(-50%);width:520px;height:420px;
  background:radial-gradient(ellipse at center,rgba(241,98,13,.20),transparent 65%);pointer-events:none}
.hero .inner{position:relative;z-index:2}
.hero .logo{width:min(560px,86vw);height:auto;margin:0 auto 40px}
.hero h1{font-size:clamp(34px,5vw,58px);max-width:19ch;margin:0 auto}
.hero h1 .g{background:linear-gradient(90deg,var(--flameL),var(--ember));-webkit-background-clip:text;background-clip:text;color:transparent}
.hero p.lead{color:var(--ash);font-size:19px;max-width:620px;margin:22px auto 0}
.hero .cta{display:flex;gap:14px;justify-content:center;flex-wrap:wrap;margin-top:34px}

/* Page hero (inner pages) */
.phero{position:relative;overflow:hidden;padding:72px 0 64px;border-bottom:1px solid var(--line)}
.phero::before{content:"";position:absolute;left:12%;top:-40%;width:700px;height:600px;
  background:radial-gradient(ellipse at center,rgba(10,111,204,.22),transparent 62%);pointer-events:none}
.phero .inner{position:relative;z-index:2;max-width:760px}
.phero h1{font-size:clamp(32px,4.6vw,52px);margin-top:14px}
.phero p{color:var(--ash);font-size:18px;margin-top:18px}
.phero .cta{display:flex;gap:14px;flex-wrap:wrap;margin-top:30px}
.crumb{font-size:13px;color:var(--ash);margin-bottom:6px}
.crumb a:hover{color:var(--blueL)}
.crumb span{margin:0 7px;opacity:.5}

/* Trust bar */
.trust{border-top:1px solid var(--line);border-bottom:1px solid var(--line);background:var(--graphite)}
.trust-grid{display:grid;grid-template-columns:repeat(4,1fr)}
.trust-grid>div{padding:26px 20px;text-align:center;border-right:1px solid var(--line)}
.trust-grid>div:last-child{border-right:none}
.trust-grid svg{width:26px;height:26px;stroke:var(--blueL);margin:0 auto 9px;fill:none;stroke-width:2}
.trust-grid h2,.trust-grid h3{font-size:14.5px;font-weight:700;color:#fff}
.trust-grid p{font-size:12.5px;color:var(--ash);margin-top:2px}

/* Services grid */
.svc-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
.svc{background:var(--panel);border:1px solid var(--line);border-radius:16px;padding:32px 28px;transition:transform .18s,border-color .18s,box-shadow .18s;display:flex;flex-direction:column}
.svc:hover{transform:translateY(-5px);border-color:rgba(18,166,240,.5);box-shadow:0 22px 50px -28px rgba(10,111,204,.7)}
.svc .ic{width:50px;height:50px;border-radius:12px;background:rgba(10,111,204,.14);display:flex;align-items:center;justify-content:center;margin-bottom:18px}
.svc .ic svg{width:25px;height:25px;stroke:var(--blueL);fill:none;stroke-width:2}
.svc h3{font-size:19px}
.svc p{font-size:14.5px;color:var(--ash);margin-top:10px;flex:1}
.svc .more{margin-top:16px;font-size:14px;font-weight:700;color:var(--flameL)}
.svc:hover .more{color:var(--flame)}

/* Why */
.why{background:var(--graphite);border-top:1px solid var(--line);border-bottom:1px solid var(--line)}
.why-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:34px}
.why-item{display:flex;gap:16px;align-items:flex-start}
.why-item .n{flex-shrink:0;width:42px;height:42px;border-radius:10px;display:flex;align-items:center;justify-content:center;
  font-size:14px;font-weight:800;color:#fff;background:linear-gradient(180deg,var(--flameL),var(--ember))}
.why-item h3{font-size:17px}
.why-item p{font-size:14px;color:var(--ash);margin-top:5px}

/* About */
.about-inner{display:grid;grid-template-columns:1.05fr .95fr;gap:56px;align-items:center}
.about-copy h2{font-size:clamp(28px,3.6vw,42px);margin-top:14px}
.about-copy p{color:var(--ash);font-size:16px;margin-top:18px}
.about-copy .pull{color:#fff;font-size:20px;font-weight:600;font-style:italic;line-height:1.5;
  border-left:3px solid var(--flame);padding-left:22px;margin-top:26px}
.about-panel{background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:40px}
.about-panel h3{font-size:12px;letter-spacing:.14em;text-transform:uppercase;color:var(--blueL);font-weight:700}
.about-panel ul{list-style:none;margin-top:22px;display:flex;flex-direction:column;gap:16px}
.about-panel li{display:flex;gap:13px;font-size:15px;color:#D5DBE1;align-items:flex-start}
.about-panel li svg{width:20px;height:20px;stroke:var(--flame);fill:none;stroke-width:2.4;flex-shrink:0;margin-top:2px}

/* Areas */
.area{background:var(--graphite);border-top:1px solid var(--line);border-bottom:1px solid var(--line)}
.area-inner{display:grid;grid-template-columns:.9fr 1.1fr;gap:52px;align-items:center}
.area h2{font-size:clamp(28px,3.6vw,42px);margin-top:14px}
.area p{color:var(--ash);font-size:16px;margin-top:16px}
.area-towns{display:flex;flex-wrap:wrap;gap:10px}
.area-towns span{border:1px solid var(--line);border-radius:30px;padding:10px 18px;font-size:14px;font-weight:600;color:#C6CFD8;transition:.15s}
.area-towns span:hover{border-color:var(--blueL);color:var(--blueL)}

/* Content prose (service + legal pages) */
.prose{max-width:820px}
.prose h2{font-size:clamp(24px,3vw,34px);margin-top:48px}
.prose h2:first-child{margin-top:0}
.prose h3{font-size:19px;margin-top:28px}
.prose p{color:var(--ash);font-size:16px;margin-top:16px}
.prose ul{list-style:none;margin-top:18px;display:flex;flex-direction:column;gap:12px}
.prose ul li{display:flex;gap:13px;font-size:15.5px;color:#D5DBE1;align-items:flex-start}
.prose ul li::before{content:"";flex-shrink:0;width:8px;height:8px;border-radius:2px;margin-top:9px;
  background:linear-gradient(180deg,var(--flameL),var(--ember))}
.prose a{color:var(--blueL);text-decoration:underline}

.steps{display:grid;grid-template-columns:repeat(4,1fr);gap:18px;margin-top:26px}
.step{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:24px 22px}
.step .n{font-size:12px;font-weight:800;letter-spacing:.1em;color:var(--flameL)}
.step h3{font-size:16px;margin-top:8px}
.step p{font-size:13.5px;color:var(--ash);margin-top:6px}

.cta-band{background:var(--graphite);border-top:1px solid var(--line);border-bottom:1px solid var(--line);text-align:center}
.cta-band h2{font-size:clamp(26px,3.4vw,38px)}
.cta-band p{color:var(--ash);margin-top:14px;font-size:17px}
.cta-band .cta{display:flex;gap:14px;justify-content:center;flex-wrap:wrap;margin-top:28px}

/* FAQ */
.faq-list{display:flex;flex-direction:column;gap:12px;max-width:900px}
details{background:var(--panel);border:1px solid var(--line);border-radius:12px;overflow:hidden}
details[open]{border-color:rgba(18,166,240,.4)}
summary{cursor:pointer;list-style:none;padding:22px 26px;font-weight:700;font-size:16.5px;display:flex;justify-content:space-between;gap:18px;align-items:center}
summary::-webkit-details-marker{display:none}
summary::after{content:"+";font-size:24px;font-weight:400;color:var(--flame);flex-shrink:0;line-height:1}
details[open] summary::after{content:"\2212"}
details p{padding:0 26px 24px;color:var(--ash);font-size:15px}

/* Contact */
.contact{background:var(--graphite);border-top:1px solid var(--line)}
.contact-inner{display:grid;grid-template-columns:1fr 1fr;gap:56px}
.contact h2{font-size:clamp(30px,4vw,46px);margin-top:14px}
.contact .lead{color:var(--ash);font-size:17px;margin-top:16px;max-width:440px}
.clines{margin-top:34px;display:flex;flex-direction:column;gap:20px}
.cline{display:flex;gap:15px;align-items:center}
.cline .ic{width:48px;height:48px;border-radius:12px;background:rgba(241,98,13,.14);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.cline .ic svg{width:21px;height:21px;stroke:var(--flame);fill:none;stroke-width:2}
.cline .t{display:block;font-size:11.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--ash);font-weight:700}
.cline .v{display:block;font-size:17px;font-weight:700;color:#fff}

form{background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:36px}
form h2,form h3{font-size:20px}
.field{margin-top:16px}
label{display:block;font-size:12px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--ash);margin-bottom:8px}
input,select,textarea{width:100%;font-family:inherit;font-size:15px;color:#fff;background:#0B0F13;
  border:1.5px solid rgba(255,255,255,.14);border-radius:9px;padding:14px 15px;transition:border-color .15s}
input:focus,select:focus,textarea:focus{outline:none;border-color:var(--blueL)}
textarea{resize:vertical;min-height:110px}
form .btn{width:100%;margin-top:20px}
.form-note{font-size:12px;color:var(--ash);margin-top:14px;text-align:center}
.form-status{margin-top:14px;padding:13px 15px;border-radius:9px;font-size:14px;font-weight:600;display:none}
.form-status.ok{display:block;background:rgba(18,166,240,.12);border:1px solid rgba(18,166,240,.45);color:#9BD6FA}
.form-status.err{display:block;background:rgba(223,46,27,.12);border:1px solid rgba(223,46,27,.5);color:#F5A79B}
.hp{position:absolute;left:-9999px;opacity:0}

/* Footer */
footer{background:var(--black);border-top:1px solid var(--line);padding:56px 0 30px;color:var(--ash)}
.foot-top{display:grid;grid-template-columns:1.5fr 1fr 1fr;gap:40px;padding-bottom:36px;border-bottom:1px solid var(--line)}
.foot-brand img{width:200px;height:auto;margin-bottom:16px}
.foot-brand p{font-size:14px;max-width:320px}
.foot-col h2{font-size:11.5px;letter-spacing:.14em;text-transform:uppercase;color:#fff;font-weight:700;margin-bottom:16px}
.foot-col a{display:block;font-size:14px;margin-bottom:10px;transition:color .15s}
.foot-col a:hover{color:var(--blueL)}
.foot-bar{display:flex;justify-content:space-between;flex-wrap:wrap;gap:14px;padding-top:24px;font-size:12.5px;align-items:center}
.gsafe{display:inline-flex;align-items:center;gap:8px;background:rgba(241,98,13,.12);border:1px solid rgba(241,98,13,.35);
  border-radius:7px;padding:7px 13px;color:var(--flameL);font-weight:700}
/* Velyntic credit */
.credit{display:flex;align-items:center;gap:10px;padding-top:22px;margin-top:18px;border-top:1px solid var(--line);
  width:100%;justify-content:center;font-size:12.5px;color:var(--ash);flex-wrap:wrap}
.credit img{height:19px;width:auto;opacity:.82;transition:opacity .15s}
.credit a{display:inline-flex;align-items:center;gap:9px}
.credit a:hover img{opacity:1}
.credit a:hover{color:var(--blueL)}

/* Sticky mobile call bar */
.callbar{display:none}
@media(max-width:760px){
  .callbar{display:flex;position:fixed;left:0;right:0;bottom:0;z-index:70;gap:10px;padding:10px 14px;
    background:rgba(0,0,0,.94);backdrop-filter:blur(10px);border-top:1px solid var(--line)}
  .callbar .btn{flex:1;padding:14px 12px;font-size:14.5px}
  body{padding-bottom:78px}
}

.reveal{opacity:0;transform:translateY(22px);transition:opacity .6s,transform .6s}
.reveal.in{opacity:1;transform:none}

@media(max-width:960px){
  .about-inner,.area-inner,.contact-inner{grid-template-columns:1fr;gap:44px}
  .svc-grid,.why-grid{grid-template-columns:1fr 1fr}
  .trust-grid{grid-template-columns:1fr 1fr}
  .trust-grid>div:nth-child(2n){border-right:none}
  .trust-grid>div:nth-child(-n+2){border-bottom:1px solid var(--line)}
  .foot-top{grid-template-columns:1fr 1fr}
  .steps{grid-template-columns:1fr 1fr}
}
@media(max-width:760px){
  nav ul,.nav-cta .phone-link{display:none}
  .burger{display:flex}
  .svc-grid,.why-grid,.foot-top,.steps{grid-template-columns:1fr}
  .sec{padding:64px 0}
  .hero{padding:64px 0 56px}
  .foot-bar{justify-content:center;text-align:center}
}
@media(prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}.reveal{opacity:1;transform:none}html{scroll-behavior:auto}}
